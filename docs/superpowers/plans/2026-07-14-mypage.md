# 마이페이지 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 헤더의 로그아웃 버튼을 마이페이지 진입점으로 바꾸고, `/mypage`에서 계좌 등록/변경·비밀번호 바꾸기·로그아웃을 제공한다.

**Architecture:** 사용자별 기본 계좌를 신규 `profiles` 테이블(RLS 본인 행)에 저장한다. `/mypage`는 로그인 필수 서버 라우트로 프로필을 읽어 폼에 프리필하고, 서버 액션으로 계좌 upsert / 비밀번호 업데이트를 처리한다. 정산 화면은 저장된 계좌를 프리필한다.

**Tech Stack:** Next.js 14 App Router, Supabase(@supabase/ssr, auth + Postgres RLS), React Server/Client Components, vitest.

## Global Constraints

- Supabase 접근: 본인 데이터 쓰기는 authed 클라이언트 `createServerSupabaseClient()`(RLS 적용)를 사용. service_role은 사용하지 않는다.
- 서버 액션 반환 타입: `type ActionResult = { success: true } | { error: string }` (기존 `app/funding/[token]/admin/actions.ts`와 동일).
- 모든 사용자 문구는 한국어.
- 마이그레이션 SQL은 자동 실행되지 않는다 — 사용자가 Supabase SQL Editor에서 직접 실행한다(기존 `supabase/migrations/*.sql`과 동일 운영).
- 커밋 메시지 말미: `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`
- 브랜치: `feature/mypage`

---

## File Structure

- Create `supabase/migrations/0002_profiles.sql` — profiles 테이블 + RLS
- Modify `lib/supabase/types.ts` — `profiles` 테이블 타입 + `Profile` 별칭
- Create `app/mypage/actions.ts` — `updateBankAccount`, `changePassword`
- Create `app/mypage/actions.test.ts` — 액션 단위 테스트
- Create `app/mypage/MyPageAccountForm.tsx` — 계좌 폼(client)
- Create `app/mypage/MyPagePasswordForm.tsx` — 비밀번호 변경 폼(client)
- Create `app/mypage/MyPageLogoutButton.tsx` — 로그아웃 버튼(client)
- Create `app/mypage/page.tsx` — 마이페이지(server, 로그인 필수)
- Modify `components/ui/Header.tsx` — 로그아웃 버튼 → 마이페이지 링크, `hideLogout`→`hideMyPage`
- Modify `app/funding/[token]/page.tsx` — `hideLogout` prop 사용처 변경
- Modify `middleware.ts` — `/mypage`를 보호 경로에 추가
- Modify `components/funding/SettleButton.tsx` — `defaultBank` prop으로 프리필
- Modify `app/funding/[token]/admin/AdminClient.tsx` — `defaultBank` 전달
- Modify `app/funding/[token]/admin/page.tsx` — 프로필 계좌 조회 후 전달

---

## Task 1: profiles 테이블 + 타입

**Files:**
- Create: `supabase/migrations/0002_profiles.sql`
- Modify: `lib/supabase/types.ts`

**Interfaces:**
- Produces: `Database['public']['Tables']['profiles']` (Row/Insert/Update), `export type Profile`. Row 필드: `user_id: string`, `bank_name: string | null`, `account_number: string | null`, `account_holder: string | null`, `updated_at: string`.

- [ ] **Step 1: 마이그레이션 SQL 작성**

Create `supabase/migrations/0002_profiles.sql`:

```sql
-- 사용자별 기본 계좌 저장용 profiles 테이블
-- Supabase SQL Editor에서 실행하세요.

create table if not exists profiles (
  user_id        uuid primary key references auth.users(id) on delete cascade,
  bank_name      text,
  account_number text,
  account_holder text,
  updated_at     timestamptz not null default now()
);

alter table profiles enable row level security;

-- 본인 행만 접근 가능
create policy "profiles_select_own" on profiles for select
  using (auth.uid() = user_id);
create policy "profiles_insert_own" on profiles for insert
  with check (auth.uid() = user_id);
create policy "profiles_update_own" on profiles for update
  using (auth.uid() = user_id);

grant select, insert, update on public.profiles to authenticated;
```

- [ ] **Step 2: 타입 추가**

In `lib/supabase/types.ts`, add the `profiles` table inside `Tables` (after the `payments` block, before `Views`):

```ts
      profiles: {
        Row: {
          user_id: string
          bank_name: string | null
          account_number: string | null
          account_holder: string | null
          updated_at: string
        }
        Insert: {
          user_id: string
          bank_name?: string | null
          account_number?: string | null
          account_holder?: string | null
          updated_at?: string
        }
        Update: {
          user_id?: string
          bank_name?: string | null
          account_number?: string | null
          account_holder?: string | null
          updated_at?: string
        }
        Relationships: []
      }
```

And add the alias near the other aliases (after `export type Payment = ...`):

```ts
export type Profile = Database['public']['Tables']['profiles']['Row']
```

- [ ] **Step 3: 타입 컴파일 확인**

Run: `npx tsc --noEmit --project tsconfig.json 2>&1 | grep -v "\.test\.ts"`
Expected: `lib/supabase/types.ts` 관련 에러 없음. (테스트 파일의 기존 top-level await 경고는 무시 — 이번 변경과 무관)

- [ ] **Step 4: Commit**

```bash
git add supabase/migrations/0002_profiles.sql lib/supabase/types.ts
git commit -m "feat(mypage): profiles 테이블 마이그레이션 및 타입 추가

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

- [ ] **Step 5: 사용자에게 마이그레이션 실행 안내**

사용자에게 알린다: "Supabase SQL Editor에서 `supabase/migrations/0002_profiles.sql`을 실행해야 마이페이지 계좌 기능이 동작합니다."

---

## Task 2: 마이페이지 서버 액션

**Files:**
- Create: `app/mypage/actions.ts`
- Test: `app/mypage/actions.test.ts`

**Interfaces:**
- Consumes: `createServerSupabaseClient` from `@/lib/supabase/server`; `profiles` 테이블(Task 1).
- Produces:
  - `updateBankAccount(bank: { bankName: string; accountNumber: string; accountHolder: string }): Promise<ActionResult>`
  - `changePassword(newPassword: string): Promise<ActionResult>`
  - `ActionResult = { success: true } | { error: string }`

- [ ] **Step 1: 실패하는 테스트 작성**

Create `app/mypage/actions.test.ts`:

```ts
import { describe, it, expect, beforeEach, vi } from 'vitest'

// createServerSupabaseClient(auth + profiles)를 인메모리 가짜로 대체한다.
// admin/actions.test.ts와 동일한 vi.mock 패턴.

type Row = Record<string, unknown>
let profilesRows: Row[]
let currentUserId: string | null
let updateUserCalledWith: { password?: string } | null
let updateUserError: { message: string } | null

vi.mock('@/lib/supabase/server', () => ({
  createServerSupabaseClient: async () => ({
    auth: {
      getUser: async () => ({ data: { user: currentUserId ? { id: currentUserId } : null } }),
      updateUser: async (attrs: { password?: string }) => {
        updateUserCalledWith = attrs
        return { data: {}, error: updateUserError }
      },
    },
    from: (table: string) => ({
      upsert: async (payload: Row) => {
        if (table !== 'profiles') return { error: { message: 'wrong table' } }
        const idx = profilesRows.findIndex((r) => r.user_id === payload.user_id)
        if (idx >= 0) profilesRows[idx] = { ...profilesRows[idx], ...payload }
        else profilesRows.push({ ...payload })
        return { error: null }
      },
    }),
  }),
}))

vi.mock('next/cache', () => ({ revalidatePath: () => {} }))

const { updateBankAccount, changePassword } = await import('./actions')

beforeEach(() => {
  currentUserId = 'user1'
  profilesRows = []
  updateUserCalledWith = null
  updateUserError = null
})

const validBank = { bankName: '토스뱅크', accountNumber: '100012345678', accountHolder: '홍길동' }

describe('updateBankAccount (계좌 저장)', () => {
  it('유효한 계좌를 저장하면 profiles에 upsert된다', async () => {
    const res = await updateBankAccount(validBank)
    expect(res).toEqual({ success: true })
    expect(profilesRows).toHaveLength(1)
    expect(profilesRows[0]).toMatchObject({
      user_id: 'user1',
      bank_name: '토스뱅크',
      account_number: '100012345678',
      account_holder: '홍길동',
    })
  })

  it('계좌 정보가 비어 있으면 저장하지 않는다', async () => {
    const res = await updateBankAccount({ bankName: '', accountNumber: '', accountHolder: '' })
    expect('error' in res).toBe(true)
    expect(profilesRows).toHaveLength(0)
  })

  it('로그인하지 않으면 저장할 수 없다', async () => {
    currentUserId = null
    const res = await updateBankAccount(validBank)
    expect('error' in res).toBe(true)
    expect(profilesRows).toHaveLength(0)
  })
})

describe('changePassword (비밀번호 변경)', () => {
  it('유효한 비밀번호로 변경하면 updateUser가 호출된다', async () => {
    const res = await changePassword('newpass123')
    expect(res).toEqual({ success: true })
    expect(updateUserCalledWith).toEqual({ password: 'newpass123' })
  })

  it('비밀번호가 8자 미만이면 변경하지 않는다', async () => {
    const res = await changePassword('short')
    expect('error' in res).toBe(true)
    expect(updateUserCalledWith).toBeNull()
  })

  it('로그인하지 않으면 변경할 수 없다', async () => {
    currentUserId = null
    const res = await changePassword('newpass123')
    expect('error' in res).toBe(true)
    expect(updateUserCalledWith).toBeNull()
  })
})
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx vitest run app/mypage/actions.test.ts`
Expected: FAIL — `./actions` 모듈이 없음(Cannot find module).

- [ ] **Step 3: 액션 구현**

Create `app/mypage/actions.ts`:

```ts
'use server'

import { revalidatePath } from 'next/cache'
import { createServerSupabaseClient } from '@/lib/supabase/server'

type ActionResult = { success: true } | { error: string }

type BankInput = {
  bankName: string
  accountNumber: string
  accountHolder: string
}

// 계좌 등록/변경: 본인 profiles 행에 기본 계좌를 upsert한다.
export async function updateBankAccount(bank: BankInput): Promise<ActionResult> {
  const supabase = await createServerSupabaseClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { error: '로그인이 필요합니다' }

  const bankName = bank.bankName?.trim()
  const accountNumber = bank.accountNumber?.trim()
  const accountHolder = bank.accountHolder?.trim()
  if (!bankName || !accountNumber || !accountHolder) {
    return { error: '계좌 정보를 모두 입력해주세요' }
  }

  const { error } = await supabase.from('profiles').upsert({
    user_id: user.id,
    bank_name: bankName,
    account_number: accountNumber,
    account_holder: accountHolder,
    updated_at: new Date().toISOString(),
  })
  if (error) return { error: error.message }

  revalidatePath('/mypage')
  return { success: true as const }
}

// 비밀번호 바꾸기: 현재 로그인 세션으로 새 비밀번호를 설정한다.
export async function changePassword(newPassword: string): Promise<ActionResult> {
  const supabase = await createServerSupabaseClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return { error: '로그인이 필요합니다' }

  if (!newPassword || newPassword.length < 8) {
    return { error: '비밀번호는 8자 이상이어야 합니다' }
  }

  const { error } = await supabase.auth.updateUser({ password: newPassword })
  if (error) {
    if (error.message?.includes('should be different')) {
      return { error: '기존 비밀번호와 다른 비밀번호를 입력해주세요' }
    }
    return { error: '비밀번호 변경에 실패했어요. 잠시 후 다시 시도해주세요' }
  }
  return { success: true as const }
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx vitest run app/mypage/actions.test.ts`
Expected: PASS (6 passed)

- [ ] **Step 5: Commit**

```bash
git add app/mypage/actions.ts app/mypage/actions.test.ts
git commit -m "feat(mypage): 계좌 저장·비밀번호 변경 서버 액션

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: 마이페이지 화면(폼 + 로그아웃 + 페이지)

**Files:**
- Create: `app/mypage/MyPageAccountForm.tsx`
- Create: `app/mypage/MyPagePasswordForm.tsx`
- Create: `app/mypage/MyPageLogoutButton.tsx`
- Create: `app/mypage/page.tsx`

**Interfaces:**
- Consumes: `updateBankAccount`, `changePassword` from `./actions` (Task 2); `signOut` from `@/app/login/actions`; `Button` from `@/components/ui/Button`.
- `MyPageAccountForm` props: `{ initial: { bankName: string; accountNumber: string; accountHolder: string } }`
- `MyPagePasswordForm` props: none
- `MyPageLogoutButton` props: none

- [ ] **Step 1: 계좌 폼 작성**

Create `app/mypage/MyPageAccountForm.tsx`:

```tsx
'use client'

import { useState, useTransition } from 'react'
import { useRouter } from 'next/navigation'
import { updateBankAccount } from './actions'

type Props = {
  initial: { bankName: string; accountNumber: string; accountHolder: string }
}

const inputCls =
  'h-[52px] rounded-xl border border-gray-200 px-4 text-sm outline-none focus:border-rose-400'

export default function MyPageAccountForm({ initial }: Props) {
  const router = useRouter()
  const [bankName, setBankName] = useState(initial.bankName)
  const [accountNumber, setAccountNumber] = useState(initial.accountNumber)
  const [accountHolder, setAccountHolder] = useState(initial.accountHolder)
  const [error, setError] = useState('')
  const [done, setDone] = useState(false)
  const [isPending, startTransition] = useTransition()

  const handleSave = () => {
    setError('')
    setDone(false)
    startTransition(async () => {
      const res = await updateBankAccount({ bankName, accountNumber, accountHolder })
      if ('error' in res) {
        setError(res.error)
        return
      }
      setDone(true)
      router.refresh()
    })
  }

  return (
    <div className="flex flex-col gap-3">
      <input
        type="text"
        value={bankName}
        onChange={(e) => setBankName(e.target.value)}
        placeholder="은행명 (예: 토스뱅크)"
        className={inputCls}
      />
      <input
        type="text"
        inputMode="numeric"
        value={accountNumber}
        onChange={(e) => setAccountNumber(e.target.value)}
        placeholder="계좌번호 ('-' 없이 숫자만)"
        className={inputCls}
      />
      <input
        type="text"
        value={accountHolder}
        onChange={(e) => setAccountHolder(e.target.value)}
        placeholder="예금주"
        className={inputCls}
      />
      {error && <p className="text-sm text-red-500">{error}</p>}
      {done && <p className="text-sm text-emerald-600">계좌가 저장되었어요 ✅</p>}
      <button
        onClick={handleSave}
        disabled={isPending}
        className="h-[52px] rounded-xl bg-rose-500 text-white text-sm font-semibold disabled:opacity-40"
      >
        {isPending ? '저장 중...' : '계좌 저장'}
      </button>
    </div>
  )
}
```

- [ ] **Step 2: 비밀번호 변경 폼 작성**

Create `app/mypage/MyPagePasswordForm.tsx`:

```tsx
'use client'

import { useState, useTransition } from 'react'
import { changePassword } from './actions'

const inputCls =
  'h-[52px] rounded-xl border border-gray-200 px-4 text-sm outline-none focus:border-rose-400'

export default function MyPagePasswordForm() {
  const [password, setPassword] = useState('')
  const [confirm, setConfirm] = useState('')
  const [error, setError] = useState('')
  const [done, setDone] = useState(false)
  const [isPending, startTransition] = useTransition()

  const handleChange = () => {
    setError('')
    setDone(false)
    if (password.length < 8) {
      setError('비밀번호는 8자 이상이어야 합니다')
      return
    }
    if (password !== confirm) {
      setError('비밀번호가 일치하지 않습니다')
      return
    }
    startTransition(async () => {
      const res = await changePassword(password)
      if ('error' in res) {
        setError(res.error)
        return
      }
      setPassword('')
      setConfirm('')
      setDone(true)
    })
  }

  return (
    <div className="flex flex-col gap-3">
      <input
        type="password"
        value={password}
        onChange={(e) => setPassword(e.target.value)}
        placeholder="새 비밀번호 (8자 이상)"
        className={inputCls}
      />
      <input
        type="password"
        value={confirm}
        onChange={(e) => setConfirm(e.target.value)}
        placeholder="새 비밀번호 확인"
        className={inputCls}
      />
      {error && <p className="text-sm text-red-500">{error}</p>}
      {done && <p className="text-sm text-emerald-600">비밀번호가 변경되었어요 ✅</p>}
      <button
        onClick={handleChange}
        disabled={isPending}
        className="h-[52px] rounded-xl bg-rose-500 text-white text-sm font-semibold disabled:opacity-40"
      >
        {isPending ? '변경 중...' : '비밀번호 변경'}
      </button>
    </div>
  )
}
```

- [ ] **Step 3: 로그아웃 버튼 작성**

Create `app/mypage/MyPageLogoutButton.tsx`:

```tsx
'use client'

import { useTransition } from 'react'
import { signOut } from '@/app/login/actions'

export default function MyPageLogoutButton() {
  const [isPending, startTransition] = useTransition()

  const handleLogout = () => {
    startTransition(async () => {
      await signOut()
      // 현재 히스토리 항목을 '/'로 교체하며 전체 새로고침한다.
      // 세션 쿠키는 signOut으로 삭제됐고, 뒤로가기로 보호 페이지에 접근해도 미들웨어가 막는다.
      window.location.replace('/')
    })
  }

  return (
    <button
      onClick={handleLogout}
      disabled={isPending}
      className="h-[52px] w-full rounded-xl border border-gray-200 text-sm font-semibold text-gray-600 disabled:opacity-40"
    >
      {isPending ? '로그아웃 중...' : '로그아웃'}
    </button>
  )
}
```

- [ ] **Step 4: 마이페이지 서버 컴포넌트 작성**

Create `app/mypage/page.tsx`:

```tsx
import { redirect } from 'next/navigation'
import { createServerSupabaseClient } from '@/lib/supabase/server'
import MyPageAccountForm from './MyPageAccountForm'
import MyPagePasswordForm from './MyPagePasswordForm'
import MyPageLogoutButton from './MyPageLogoutButton'

export default async function MyPage() {
  const supabase = await createServerSupabaseClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) redirect('/login')

  const { data: profile } = await supabase
    .from('profiles')
    .select('bank_name, account_number, account_holder')
    .eq('user_id', user.id)
    .maybeSingle()

  return (
    <main className="px-4 py-6 flex flex-col gap-8">
      <h1 className="text-xl font-bold text-gray-900">마이페이지</h1>

      <section className="flex flex-col gap-3">
        <div>
          <h2 className="text-base font-semibold text-gray-800">정산 계좌</h2>
          <p className="text-sm text-gray-400">펀딩 정산 시 자동으로 채워져요</p>
        </div>
        <MyPageAccountForm
          initial={{
            bankName: profile?.bank_name ?? '',
            accountNumber: profile?.account_number ?? '',
            accountHolder: profile?.account_holder ?? '',
          }}
        />
      </section>

      <section className="flex flex-col gap-3">
        <h2 className="text-base font-semibold text-gray-800">비밀번호 바꾸기</h2>
        <MyPagePasswordForm />
      </section>

      <section className="flex flex-col gap-3">
        <MyPageLogoutButton />
      </section>
    </main>
  )
}
```

- [ ] **Step 5: 빌드 확인**

Run: `npm run build`
Expected: `✓ Compiled successfully`, 라우트 목록에 `/mypage` 표시.

- [ ] **Step 6: Commit**

```bash
git add app/mypage/MyPageAccountForm.tsx app/mypage/MyPagePasswordForm.tsx app/mypage/MyPageLogoutButton.tsx app/mypage/page.tsx
git commit -m "feat(mypage): 마이페이지 화면(계좌/비밀번호/로그아웃)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: Header — 로그아웃 버튼을 마이페이지 링크로 교체

**Files:**
- Modify: `components/ui/Header.tsx`
- Modify: `app/funding/[token]/page.tsx`

**Interfaces:**
- `Header` props 변경: `hideLogout?: boolean` → `hideMyPage?: boolean`. (`backHref?`, `right?`는 유지)

- [ ] **Step 1: Header에서 로그아웃 로직 제거하고 마이페이지 링크 추가**

In `components/ui/Header.tsx`:

Replace the imports block (top of file) — remove `useTransition` and the `signOut` import:

```tsx
'use client'

import { useEffect, useState } from 'react'
import { useRouter } from 'next/navigation'
import Link from 'next/link'
import Image from 'next/image'
import { createClient } from '@/lib/supabase/client'
```

Replace the `Props` type:

```tsx
type Props = {
  backHref?: string
  right?: React.ReactNode
  hideMyPage?: boolean
}
```

Replace the component signature and remove `isPending`/`handleSignOut`:

```tsx
export default function Header({ backHref, right, hideMyPage }: Props) {
  const router = useRouter()
  const [isLoggedIn, setIsLoggedIn] = useState(false)

  useEffect(() => {
    const supabase = createClient()
    supabase.auth.getUser().then(({ data }) => setIsLoggedIn(!!data.user))

    const {
      data: { subscription },
    } = supabase.auth.onAuthStateChange((_event, session) => {
      setIsLoggedIn(!!session?.user)
    })
    return () => subscription.unsubscribe()
  }, [])
```

(Delete the entire `const handleSignOut = () => { ... }` block.)

Replace the right-side cluster JSX:

```tsx
        {(right || (isLoggedIn && !hideMyPage)) && (
          <div className="flex items-center gap-2">
            {right}
            {isLoggedIn && !hideMyPage && (
              <Link
                href="/mypage"
                className="text-sm text-gray-400 hover:text-gray-700 hover:underline"
              >
                마이페이지
              </Link>
            )}
          </div>
        )}
```

- [ ] **Step 2: 상세 페이지의 prop 사용처 변경**

In `app/funding/[token]/page.tsx`, change `hideLogout` to `hideMyPage` in the `<Header ... />` usage:

```tsx
      <Header
        backHref="/funding"
        hideMyPage
        right={
```

(only the `hideLogout` → `hideMyPage` line changes; `right` block unchanged.)

- [ ] **Step 3: 빌드 확인**

Run: `npm run build`
Expected: `✓ Compiled successfully`, 타입 에러 없음(특히 `hideLogout` 미사용 확인).

- [ ] **Step 4: Commit**

```bash
git add components/ui/Header.tsx "app/funding/[token]/page.tsx"
git commit -m "feat(header): 로그아웃 버튼을 마이페이지 링크로 교체

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: 미들웨어 — /mypage 보호

**Files:**
- Modify: `middleware.ts`

**Interfaces:**
- Consumes: 없음. `/mypage`를 비로그인 시 `/login`으로 리다이렉트.

- [ ] **Step 1: 보호 경로에 /mypage 추가**

In `middleware.ts`, update the `isProtected` expression:

```ts
  const { pathname } = request.nextUrl;
  const isProtected =
    pathname.startsWith("/create") ||
    pathname.startsWith("/mypage") ||
    /^\/funding\/[^/]+\/(admin|edit)/.test(pathname);
```

- [ ] **Step 2: 동작 확인 (dev 스모크)**

Run (dev 서버가 3000에서 실행 중이라고 가정):
`curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/mypage`
Expected: `307` (비로그인 → /login 리다이렉트)

- [ ] **Step 3: Commit**

```bash
git add middleware.ts
git commit -m "feat(mypage): /mypage를 로그인 보호 경로에 추가

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 6: 정산 계좌 프리필

**Files:**
- Modify: `components/funding/SettleButton.tsx`
- Modify: `app/funding/[token]/admin/AdminClient.tsx`
- Modify: `app/funding/[token]/admin/page.tsx`

**Interfaces:**
- `SettleButton` props에 `defaultBank?: { bankName: string; accountNumber: string; accountHolder: string }` 추가.
- `AdminClient` props에 `defaultBank?: { bankName: string; accountNumber: string; accountHolder: string }` 추가.

- [ ] **Step 1: SettleButton이 defaultBank로 초기화되도록 수정**

In `components/funding/SettleButton.tsx`, add to the `Props` type:

```tsx
  defaultBank?: {
    bankName: string
    accountNumber: string
    accountHolder: string
  }
```

Update the signature and the three `useState` initializers:

```tsx
export default function SettleButton({ fundingId, status, totalAmount, settledInfo, defaultBank }: Props) {
  const router = useRouter()
  const [showModal, setShowModal] = useState(false)
  const [bankName, setBankName] = useState(defaultBank?.bankName ?? '')
  const [accountNumber, setAccountNumber] = useState(defaultBank?.accountNumber ?? '')
  const [accountHolder, setAccountHolder] = useState(defaultBank?.accountHolder ?? '')
```

(rest of the component unchanged.)

- [ ] **Step 2: AdminClient가 defaultBank를 SettleButton으로 전달하도록 수정**

In `app/funding/[token]/admin/AdminClient.tsx`, add to `Props`:

```tsx
  defaultBank?: {
    bankName: string;
    accountNumber: string;
    accountHolder: string;
  };
```

Update the signature:

```tsx
export default function AdminClient({
  funding,
  payments,
  totalAmount,
  defaultBank,
}: Props) {
```

Pass it to `SettleButton` (add the prop to the existing usage):

```tsx
        <SettleButton
          fundingId={funding.id}
          status={funding.status}
          totalAmount={totalAmount}
          defaultBank={defaultBank}
          settledInfo={{
            settledAmount: funding.settled_amount,
            bankName: funding.settle_bank_name,
            accountNumber: funding.settle_account_number,
            accountHolder: funding.settle_account_holder,
            settledAt: funding.settled_at,
          }}
        />
```

- [ ] **Step 3: admin 서버 페이지에서 프로필 계좌 조회 후 전달**

In `app/funding/[token]/admin/page.tsx`, after computing `totalAmount` and before `return`, fetch the owner's profile using the authed client (RLS 본인 행):

```tsx
  const { data: profile } = await serverClient
    .from('profiles')
    .select('bank_name, account_number, account_holder')
    .eq('user_id', user.id)
    .maybeSingle()

  const defaultBank = profile
    ? {
        bankName: profile.bank_name ?? '',
        accountNumber: profile.account_number ?? '',
        accountHolder: profile.account_holder ?? '',
      }
    : undefined
```

Update the render:

```tsx
  return (
    <AdminClient
      funding={funding}
      payments={payments ?? []}
      totalAmount={totalAmount}
      defaultBank={defaultBank}
    />
  )
```

(`serverClient` is the existing authed client already created at the top of the file; reuse it — do not use `createServiceClient` for profiles.)

- [ ] **Step 4: 빌드 확인**

Run: `npm run build`
Expected: `✓ Compiled successfully`, 타입 에러 없음.

- [ ] **Step 5: Commit**

```bash
git add components/funding/SettleButton.tsx "app/funding/[token]/admin/AdminClient.tsx" "app/funding/[token]/admin/page.tsx"
git commit -m "feat(mypage): 정산 계좌를 저장된 프로필로 프리필

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 7: 통합 수동 검증

**Files:** 없음(수동 확인)

- [ ] **Step 1: 마이그레이션 적용 확인**

사용자가 Supabase SQL Editor에서 `0002_profiles.sql`을 실행했는지 확인한다. 미적용 시 계좌 저장이 실패하므로 반드시 선행.

- [ ] **Step 2: 전체 흐름 스모크**

dev 서버(`npm run dev`, 3000)에서 로그인 후 확인:
1. 헤더에 "마이페이지" 링크 표시 → 클릭 시 `/mypage` 진입
2. 계좌 입력 → "계좌 저장" → 새로고침 후에도 값 유지
3. 새 비밀번호(8자 이상) + 확인 일치 → "비밀번호 변경" 성공 메시지
4. 마감된 펀딩의 관리(admin) → 정산 모달에 저장한 계좌가 프리필되는지
5. 로그아웃 → 메인('/')으로 이동, 브라우저 뒤로가기로 `/mypage`·`/create` 접근 시 `/login`으로 리다이렉트

- [ ] **Step 3: 비로그인 방어 확인**

로그아웃 상태로 `http://localhost:3000/mypage` 직접 접근 → `/login`으로 리다이렉트되는지 확인.

---

## Self-Review

**Spec coverage:**
- Header 로그아웃→마이페이지 링크 → Task 4 ✓
- `/mypage` 페이지(로그인 필수) → Task 3(page redirect) + Task 5(middleware) ✓
- 계좌 등록/변경 + profiles 테이블 → Task 1(table) + Task 2(action) + Task 3(form) ✓
- 비밀번호 바꾸기 → Task 2(action) + Task 3(form) ✓
- 로그아웃(히스토리 교체) → Task 3(MyPageLogoutButton) ✓
- 정산 프리필 → Task 6 ✓
- 대기 중 로그아웃 로직 이전: Header에서 제거(Task 4) + MyPageLogoutButton으로 이동(Task 3) ✓

**Placeholder scan:** 모든 스텝에 실제 코드/명령 포함. 플레이스홀더 없음.

**Type consistency:** `defaultBank`의 형태 `{ bankName; accountNumber; accountHolder }`가 SettleButton·AdminClient·admin/page에서 일치. `ActionResult` 형태가 액션·폼에서 일치. `Profile`/`profiles` 필드명(`bank_name`, `account_number`, `account_holder`)이 마이그레이션·타입·액션·조회에서 일치.
